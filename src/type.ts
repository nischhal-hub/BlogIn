export type ItemProps = {
    id: string;
    title: string;
    image: string;
    authorName: string;
    createdAt: string;
  }

export type FormFields = {
    title: string;
    overview: string;
    image: File;
}